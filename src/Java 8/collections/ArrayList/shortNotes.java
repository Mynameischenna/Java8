package collections.ArrayList;

public class shortNotes {
	
	
//	1. Initialization
//
//	ArrayList<String> list = new ArrayList<>(); // Default constructor
//	ArrayList<String> listWithCapacity = new ArrayList<>(10); // With initial capacity
//	ArrayList<String> listFromCollection = new ArrayList<>(anotherCollection); // From another collection

//	2.Adding elements
//	list.add("Element"); // Add element to the end
//	list.add(1, "Element"); // Add element at specific index
//	list.addAll(anotherList); // Add all elements from another collection
//	list.addAll(1, anotherList); // Add all elements from another collection starting from a specific index

//	3.Accessing elements:
//		String element = list.get(0); // Get element at specific index
//	int size = list.size(); // Get the size of the list
//	boolean isEmpty = list.isEmpty(); // Check if the list is empty
//	boolean contains = list.contains("Element"); // Check if the list contains a specific element
//	int index = list.indexOf("Element"); // Get the first index of a specific element
//	int lastIndex = list.lastIndexOf("Element"); // Get the last index of a specific element

//	4. Updating Elements
//	list.set(0, "New Element"); // Set element at specific index

//	5. Removing Elements
//	list.remove(0); // Remove element at specific index
//	list.remove("Element"); // Remove the first occurrence of a specific element
//	list.removeAll(anotherList); // Remove all elements that are in another collection
//	list.clear(); // Remove all elements from the list

	
//	7. Sorting and Shuffling
//	Collections.sort(list); // Sort list in natural order
//	Collections.sort(list, comparator); // Sort list using a custom comparator
//	Collections.shuffle(list); // Shuffle elements in the list randomly

	

}
